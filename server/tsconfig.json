{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // These settings are inherited from the root tsconfig.json file
    // "module": "nodenext",
    // "moduleResolution": "nodenext",
    "outDir": "dist",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@shared/*": ["../shared/*"]
    }
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules"]
}
